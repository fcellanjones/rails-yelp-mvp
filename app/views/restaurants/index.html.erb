<h1>Restaurants</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
  <li> <%= restaurant.name %>: <%= restaurant.category %> -  <%= restaurant.address %>, <%= restaurant.phone_number %></li>
  <%= link_to "See details", restaurant_path(restaurant), class: "btn btn-primary"%>
  <% end %>
</ul>


